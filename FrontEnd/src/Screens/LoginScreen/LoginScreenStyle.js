import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        marginTop: 35,
        height: 42,
        borderColor: '#ADB5BD',
        borderWidth: 1,
        borderRadius: 4,
    }
})